syntax = "proto3";

package me.kasuki.kstaff.api.database.redis.event;

message Event {
  EventType eventType = 1;
  bytes eventData = 2;
}

message EventBundle {
  repeated Event events = 1;
}

enum EventType {
  SERVER_UPDATE = 0;
}